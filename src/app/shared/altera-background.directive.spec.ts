import { AlteraBackgroundDirective } from './altera-background.directive';

describe('AlteraBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new AlteraBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
